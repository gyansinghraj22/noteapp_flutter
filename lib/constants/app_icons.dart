import 'package:flutter/material.dart';

class AppIcons {
  static const Icon errorIcon = Icon(Icons.error);
  static const Icon arrowBack = Icon(Icons.arrow_back);
  static const Icon search = Icon(Icons.search);

}
  